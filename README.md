# CloudGAN: Generating atmospheric cloud fields using generative adversarial networks

This project investigates the potential of using generative adversarial networks
(GANs) to generate cloud fields.
